{
  "name": "gmail-mcp-server",
  "version": "1.0.0",
  "description": "MCP server for Gmail operations - read, write, send, manage emails",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsc && node dist/index.js",
    "inspect": "tsc && node --inspect dist/index.js",
    "setup": "tsc && node dist/setup.js",
    "test": "tsc && node dist/test.js"
  },
  "keywords": [
    "mcp",
    "gmail",
    "email",
    "automation",
    "n8n"
  ],
  "author": "Gmail MCP Server",
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^0.5.0",
    "googleapis": "^140.0.1",
    "google-auth-library": "^9.0.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.3.0"
  }
}